# ============================================
# APPLICATION SETTINGS
# ============================================
APP_NAME=Evidence Analysis System
APP_VERSION=1.0.0
DEBUG=False

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://evidence_user:your_password_here@localhost:5432/evidence_analysis

# ============================================
# JWT AUTHENTICATION
# ============================================
# Generate a secure secret key (use: openssl rand -hex 32)
JWT_SECRET_KEY=your_jwt_secret_key_here_use_openssl_rand_hex_32
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ============================================
# CORS ORIGINS
# ============================================
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# FILE STORAGE CONFIGURATION
# ============================================
# Storage type: "gcp", "s3", or "local"
STORAGE_TYPE=gcp

# Cloud Storage Provider: "gcp" or "aws"
CLOUD_STORAGE_PROVIDER=gcloud

# GCP Storage Configuration (if STORAGE_TYPE=gcp)
CLOUD_STORAGE=GCP
CLOUD_STORAGE_ACCOUNTNAME=your-service-account@your-project.iam.gserviceaccount.com
CLOUD_STORAGE_BUCKETNAME=your-bucket-name
CLOUD_STORAGE_BUCKET_TYPE=private
CLOUD_STORAGE_PROJECT=your-gcp-project-id
CLOUD_STORAGE_SECRET=-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----

# AWS S3 Configuration (if STORAGE_TYPE=s3)
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1
S3_BUCKET_NAME=evidence-analysis-files

# Local Storage Configuration (if STORAGE_TYPE=local)
LOCAL_STORAGE_PATH=./uploads

# ============================================
# AI MODEL CONFIGURATION (Google Gemini)
# ============================================
# Multiple API keys for rotation (at least one required)
GEMINI_API_KEY_1=your_gemini_api_key_1
GEMINI_API_KEY_2=your_gemini_api_key_2
GEMINI_API_KEY_3=your_gemini_api_key_3
GEMINI_MODEL=gemini-2.5-flash

# ============================================
# EMAIL CONFIGURATION (SMTP)
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_specific_password
SMTP_FROM_EMAIL=noreply@evidence-analysis.com
SMTP_FROM_NAME=Evidence Analysis System

# ============================================
# BACKGROUND PROCESSING
# ============================================
MAX_CONCURRENT_JOBS=5
WORKER_CHECK_INTERVAL=5

# ============================================
# FILE UPLOAD LIMITS
# ============================================
MAX_UPLOAD_SIZE=104857600
ALLOWED_EXTENSIONS=.csv
